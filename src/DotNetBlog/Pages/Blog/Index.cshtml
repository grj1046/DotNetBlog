@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}
<h2>@ViewData["Title"]</h2>
<ul>
    @foreach (var item in Model.Posts)
    {
        <li><a asp-page="Post" asp-route-p="@item.URL">@item.Title</a></li>
    }
</ul>